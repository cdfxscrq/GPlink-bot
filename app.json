{
    "name": "GPlink_Bot",
    "description": "Telegram's best Open Source ALL-In-One Multi Purpose RoBot. Create your Fork now.",
    "keywords": [
        "telegram",
        "gplink",
        "bot",
        "opensource"
    ],
    "website": "https://github.com/Mahesh0253/GPlink-bot",
    "repository": "https://github.com/cdfxscrq/GPlink-bot",
    "version": "2.0.0",
    "env": {
        "BOT_TOKEN": {
            "description": "Your Telegram bot token from @BotFather",
            "required": true
        },
        "API_ID": {
            "description": "Get this value from https://my.telegram.org (API development tools)",
            "required": true
        },
        "API_HASH": {
            "description": "Get this value from https://my.telegram.org (API development tools)",
            "required": true
        },
        "API_KEY": {
            "description": "Get API key from https://gplinks.in/member/tools/api",
            "required": false
        }
    },
    "addons": [],
    "buildpacks": [
        {
            "url": "heroku/python"
        }
    ],
    "formation": {
        "worker": {
            "quantity": 1,
            "size": "free"
        }
    }
}
